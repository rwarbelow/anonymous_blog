<h1>Posts matching tag: <%= @tag.tag_description %></h1>
  <% if @posts %>
  <table>
    <% @posts.each do |post| %>
  <tr>
  <td><a href="/show/<%= post.id %>"><%= post.title %></a></td>
  </tr>
  <% end %>
  </table>
  <% else %>
    <p>No matching results</p>
  <% end %>
